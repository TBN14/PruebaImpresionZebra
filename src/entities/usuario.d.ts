export interface IUsuario {
  name: string;
  email: string;
  contacto: string;
  direccion: string;
  sexo: string;
  raza: 'negro' | 'blanco';
  // height: `${string}cm`;
  // weight: `${string}kg`;
  // id: string;
}
